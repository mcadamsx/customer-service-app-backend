[general]
ignore-merge-commits=true

[title-match-regex]
regex=^(feat|fix|doc|chore|test|refactor): .+
# Note: gitlint doesn’t support custom error-msg here, it will show default message

[body-min-length]
min-length=1
